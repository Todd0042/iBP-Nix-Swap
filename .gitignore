# Hardware-specific, generated by nixos-generate-config on a new install.
hardware-configuration.nix
# GPU module also lives outside git — see README.
gpu.nix

# nix
result
result-*
.direnv/

# snapshot artifacts
home-snapshot-*.tar.zst
*.tar.zst

# editors
.vscode/
.idea/
